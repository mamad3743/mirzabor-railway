# دیپلوی MirzaBot روی Railway

این پروژه اصلش برای نصب روی یک VPS خام با اسکریپت `install.sh` طراحی شده
(نصب خودکار Apache/PHP/MySQL روی سرور). فایل‌های زیر به پروژه اضافه شده‌اند
تا بدون نیاز به آن اسکریپت، همه‌چیز داخل یک کانتینر Docker روی Railway بالا بیاید:

- `Dockerfile` — PHP 8.2 + Apache + همه‌ی اکستنشن‌های لازم (gd, zip, intl, mysqli,
  pdo_mysql, imagick, curl, bcmath) + نصب خودکار composer
- `docker/entrypoint.sh` — هنگام استارت کانتینر:
  - `config.php` را از روی Environment Variableها می‌سازد
  - پورت Apache را با `$PORT` که Railway می‌دهد هماهنگ می‌کند
  - منتظر بالا آمدن دیتابیس می‌ماند و `table.php` را اجرا می‌کند (ساخت/آپدیت جداول)
  - وبهوک تلگرام را به صورت خودکار روی دامنه‌ی Railway ست می‌کند
  - کران‌جاب‌های داخلی ربات (statusday، activeconfig، expireagent، backup و ...)
    را با `cron` داخل همان کانتینر راه می‌اندازد (چون روی Railway کرانتب سیستمی
    مثل VPS در دسترس نیست)
- `railway.json` — به Railway می‌گوید از همین `Dockerfile` بیلد کند
- یک اصلاح کوچک در `function.php` (تابع `checktelegramip`) تا IP واقعی تلگرام
  را از هدر `X-Forwarded-For` بخواند؛ چون پشت پراکسی Railway مقدار `REMOTE_ADDR`
  دیگر IP واقعی تلگرام نیست و بدون این اصلاح وبهوک همیشه Unauthorized می‌داد.

## مراحل دیپلوی

1. **ریپو را روی Railway وصل کنید**
   یک پروژه جدید در Railway بسازید → `Deploy from GitHub repo` (یا از همین پوشه
   یک ریپوی گیت بسازید و push کنید). Railway به‌خاطر وجود `Dockerfile` خودش
   حالت Dockerfile build را انتخاب می‌کند.

2. **یک دیتابیس MySQL اضافه کنید**
   داخل همان پروژه Railway → `+ New` → `Database` → `Add MySQL`.
   Railway به‌صورت خودکار متغیرهای `MYSQLHOST`, `MYSQLPORT`, `MYSQLDATABASE`,
   `MYSQLUSER`, `MYSQLPASSWORD` را در اختیار سرویس بات می‌گذارد — نیازی نیست
   خودتان چیزی برای دیتابیس ست کنید.

3. **Public Networking را برای سرویس بات فعال کنید**
   در تنظیمات سرویس (Settings → Networking) یک دامنه‌ی عمومی بسازید
   (`*.up.railway.app` یا دامنه‌ی خودتان). همین دامنه به‌صورت خودکار در
   `RAILWAY_PUBLIC_DOMAIN` قرار می‌گیرد و entrypoint از همان برای ست کردن
   وبهوک و مقدار `domainhosts` استفاده می‌کند.

4. **این Environment Variableها را برای سرویس ست کنید:**

   | نام متغیر      | توضیح                                                        |
   |----------------|---------------------------------------------------------------|
   | `BOT_TOKEN`    | توکن ربات تلگرام (از BotFather)                               |
   | `ADMIN_ID`     | آیدی عددی چت ادمین اصلی در تلگرام                              |
   | `BOT_USERNAME` | یوزرنیم ربات بدون `@`                                          |
   | `DOMAIN`       | اختیاری — اگر ست نشود از `RAILWAY_PUBLIC_DOMAIN` استفاده می‌شود |

   (متغیرهای دیتابیس را دست نزنید — از پلاگین MySQL می‌آیند.)

5. **Deploy کنید.** بعد از بالا آمدن سرویس:
   - جداول دیتابیس خودکار ساخته می‌شوند
   - وبهوک تلگرام خودکار روی `https://<دامنه>/index.php` ست می‌شود
   - برای شروع، در تلگرام به ربات دستور `/start` بدهید

## ورود به پنل ادمین وب

پنل مدیریت وب روی مسیر `/panel` بالا می‌آید. نام‌کاربری پیش‌فرض `admin` است و
رمز عبور یک رشتهٔ تصادفی است که هنگام اولین اجرای `table.php` داخل جدول
`admin` در دیتابیس ذخیره می‌شود. برای دیدن یا تغییر آن می‌توانید یک بار به
دیتابیس (از داشبورد Railway → سرویس MySQL → Data) وصل شوید و مقدار ستون
`password` در جدول `admin` را بخوانید یا مقدار دلخواه خودتان را جایگزین کنید.

## نکات مهم

- **درگاه‌های پرداخت / پنل‌های VPN**: تنظیمات مربوط به پنل‌ها (Marzban،
  Sanaei و ...) و درگاه‌های پرداخت همچنان از داخل خود ربات (منوی ادمین) انجام
  می‌شود، همان‌طور که در نصب معمولی هست.
- **آپدیت نسخه**: کافیست کد جدید را push کنید و Railway ری‌دیپلوی می‌کند؛
  `table.php` روی هر استارت دوباره اجرا می‌شود ولی چون قبل از ساخت هر جدول
  چک می‌کند که جدول موجود است یا نه، داده‌های قبلی پاک نمی‌شود.
- **پرسیستنت بودن فایل‌ها**: کانتینرهای Railway استوریج پرسیستنت روی دیسک
  کانتینر ندارند (هر دیپلوی جدید = فایل‌سیستم تازه). اگر از قابلیت بکاپ
  ربات (`cronbot/backupbot.php`) یا آپلود فایل داخل پوشه‌ی پروژه استفاده
  می‌کنید، برای نگه‌داشتن آن‌ها باید یک [Railway Volume](https://docs.railway.app/reference/volumes)
  به مسیر مناسب (مثلاً `/var/www/html/storage`) وصل کنید.
